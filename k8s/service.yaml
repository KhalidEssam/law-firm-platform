apiVersion: v1
kind: Service
metadata:
  name: exoln-lex-api
  namespace: exoln-lex
  labels:
    app.kubernetes.io/name: exoln-lex
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: exoln-lex
    app.kubernetes.io/component: api
